\section{Notation}
\label{sec:notation}

This section defines the notation used in subsequent sections.

Unless stated otherwise, every quantity is an 18-decimal fixed-point
integer (WAD, scale $10^{18}$): NAV amounts, rates, ratios, senior
tranche shares, and BPT amounts alike. The single exception is raw
quote-asset amounts, which keep their token's native decimals (e.g.\
$10^6$ for USDC).

{\small
\setlength{\tabcolsep}{4pt}
\begin{xltabular}{\textwidth}{l X l}
  \caption{Mathematical notation used throughout this document.}
  \label{tbl:notation-math} \\
  \toprule
  \textbf{Symbol} & \textbf{Meaning} & \textbf{Units} \\
  \midrule
  \endfirsthead
  \toprule
  \textbf{Symbol} & \textbf{Meaning} & \textbf{Units} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  \bottomrule
  \endlastfoot
  \multicolumn{3}{@{}l}{\emph{Tranche NAVs and supply}} \\
  \midrule
  $\STraw$           & Raw NAV of the senior tranche's own assets                                       & NAV \\
  $\JTraw$           & Raw NAV of the junior tranche's BPT collateral, $\JTraw = P_A(y)$               & NAV \\
  $\STeff$           & Senior effective NAV after the waterfall                                         & NAV \\
  $\JTeff$           & Junior effective NAV after the waterfall                                         & NAV \\
  $y$                & Senior rate (NAV per senior share), the candidate $y = x/N$ fed into the solve  & NAV/share \\
  $y^\star$          & Published senior rate, $y^\star = x^\star/N$ rounded toward senior, $= \STeff/N$ at the largest fixed point & NAV/share \\
  $y_0$              & Cached senior rate, read once at solve start (fixes $L$ and the band)            & NAV/share \\
  $x$                & Candidate senior NAV (the bisection probe), self-consistent exactly where $E(x) = 0$. The largest such point is the published $x^\star$ & NAV \\
  $N$                & Total senior-share supply (including any fee shares), frozen at solve start. The same $N$ appears in the clamp and the rate & shares \\
  \midrule
  \multicolumn{3}{@{}l}{\emph{The fixed-point machinery}} \\
  \midrule
  $F$                & Sync map, $(\STraw,\ \JTraw;\ \text{checkpoint}) \mapsto (\STeff,\ \JTeff)$, read as $\JTraw \mapsto \STeff$ once the rest is fixed (\cref{sec:setup:F})      & function \\
  checkpoint         & Previous sync's raw NAVs, effective NAVs, outstanding ILs, market state, and yield-accrual bookkeeping & --- \\
  $P_A$              & Conservative BPT valuation, $\JTraw = P_A(y)$                                    & function \\
  $E$                & Error function, $E(x) = F(P_A(x/N)) - x$, the bisection target                  & function \\
  $g$                & Loop gain (round-trip amplification of $x$). The clamp at $N$ gives $g = f$, and $f \leq 1$ since the junior owns at most the whole pool  & dimensionless \\
  \midrule
  \multicolumn{3}{@{}l}{\emph{Pool reserves and geometry (Gyro E-CLP)}} \\
  \midrule
  $b_q$              & Quote-leg reserve at the fair point                                             & NAV \\
  $q$                & Quote-asset rate ($1$ for non-rate-bearing quote tokens)                         & NAV/quote \\
  $f$                & JT's fractional share of the total BPT supply                                    & dimensionless \\
  $\alpha$, $\beta$  & Per-solve lower / upper bounds on the senior rate the pool can quote, derived from the configured E-CLP band (\cref{sec:solver:bracket}, distinct from Royco's correlation parameter) & NAV/share \\
  $L$                & E-CLP invariant value                         & --- \\
  $V_{\max}$         & Pool value at the all-quote corner of the frozen curve, $V_{\max} = \sup_y V(y)$                  & NAV \\
  $V(y)$             & Fair pool value at marginal price $y$ (envelope-theorem valuation)               & NAV \\
  $D(y)$             & The pool's demand for senior shares at price $y$ (the fair reserve), $D(y) = V'(y)$ & shares \\
\end{xltabular}}
